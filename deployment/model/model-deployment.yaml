apiVersion: apps/v1
kind: Deployment
metadata:
  name: dog-prediction-model
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dog-prediction-model
  template:
    metadata:
      labels:
        app: dog-prediction-model
    spec:
      containers:
      - name: dog-prediction-model
        image: dog-prediction-model:v1
        #image: {AWS_ACCOUNT_ID}.dkr.ecr.{REGION}.amazonaws.com/dog-prediction-images:dog-prediction-model
        resources:
          limits:
            memory: "1000Mi"
            cpu: "200m"
        ports:
        - containerPort: 8500